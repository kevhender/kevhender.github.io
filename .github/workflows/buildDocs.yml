name: Build API Docs

on: push

jobs:
  test_build:
    runs-on: ubuntu-latest

    name: Testing the build
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
      - run: npm run build
